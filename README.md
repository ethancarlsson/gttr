# Gttr
`gttr` (or getter) is a generator command for creating getters for structs with internal values.
This is so devs can create values that are readonly from outside their modules.

##  Why?
It's a quick way to get variables that are read only from outside the module.

## Status
In development.
